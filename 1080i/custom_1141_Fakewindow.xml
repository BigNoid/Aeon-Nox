<?xml version="1.0" encoding="utf-8"?>
<window id="1141" type="window">
    <defaultcontrol always="true">9000</defaultcontrol>
    <onload condition="Stringcompare(Window(home).Property(activewindow),videos)">ReplaceWindow(videos,$INFO[Window(home).Property(containerpath)],return)</onload>
    <onload condition="Stringcompare(Window(home).Property(activewindow),addonbrowser)">ReplaceWindow(addonbrowser)</onload>
    <onload condition="Stringcompare(Window(home).Property(activewindow),programs)">ReplaceWindow(programs)</onload>
    <onload condition="Stringcompare(Window(home).Property(activewindow),musiclibrary)">ReplaceWindow(musiclibrary,$INFO[Window(home).Property(containerpath)],return)</onload>
    <onload condition="Stringcompare(Window(home).Property(activewindow),musicfiles)">ReplaceWindow(musicfiles)</onload>
    <onload condition="Stringcompare(Window(home).Property(activewindow),musicplaylist)">ReplaceWindow(musicplaylist)</onload>
    <onload condition="Stringcompare(Window(home).Property(activewindow),videoplaylist)">ReplaceWindow(videoplaylist)</onload>
    <!-- 	<onload condition="IsEmpty(Window(home).Property(activewindow))">ReplaceWindow(home)</onload>
 -->
    <coordinates>
        <posx>0</posx>
        <posy>0</posy>
        <system>1</system>
    </coordinates>
    <controls>
        <control type="button" id="9000">
            <include>HiddenObject</include>
        </control>
    </controls>
</window>